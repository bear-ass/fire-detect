# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=fire_monitoring
DB_USER=root
DB_PASSWORD=your_password

# 应用配置
FLASK_ENV=development
SECRET_KEY=dev-secret-key-change-in-production

# NASA API配置（可选）
CRAWL_INTERVAL_HOURS=3